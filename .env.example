# Copy to .env.local for local `vercel dev` testing.
# In production, set these in the Vercel dashboard (see deployment instructions).

GROQ_API_KEY=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
ALLOWED_ORIGIN=*
